# ESP-Ditto 首批 PCBA 下单闸门

只有所有 `BLOCKER` 项关闭后才付款下单。

## 版本冻结

- [ ] `BLOCKER` 已记录立创开源硬件工程更新时间和克隆日期。
- [ ] `BLOCKER` 已记录 ESP-CLAW commit：`________________`。
- [ ] `BLOCKER` 已保存未修改的原始工程副本。
- [ ] 已建立个人工作副本：`ESP-Ditto_rev-____`。

## 主控与固件

- [ ] `BLOCKER` U1 精确目标料号为 `ESP32-C5-WROOM-1-N16R8`，作者或验证记录未提出冲突。
- [ ] `BLOCKER` Flash：`16 MB`，PSRAM：`8 MB`，与固件配置一致。
- [ ] `BLOCKER` 模组封装、天线端方向和禁布区与 PCB 一致。
- [ ] `BLOCKER` ESP-IDF 目标为 `esp32c5`，`esp_Ditto` 板级配置可生成。
- [ ] `BLOCKER` 固件已本地编译成功，分区表为：`________________`。

## 设计检查

- [ ] `BLOCKER` 原理图与 PCB 网表一致。
- [ ] `BLOCKER` DRC 已通过；例外清单路径：`________________`。
- [ ] `BLOCKER` Gerber 独立复核完成，确认四层、板框、槽孔和钻孔。
- [ ] `BLOCKER` CPL 的顶/底面及器件旋转已复核。
- [ ] 3D 预览中连接器、按键、FPC 和 USB 方向正确。

## BOM 与 SMT

- [ ] `BLOCKER` BOM、CPL、Gerber 来自同一工程 revision。
- [ ] `BLOCKER` U1、BMM150、BMI270、QMC6309、Si12T、BQ27220、FPC 座均有精确料号。
- [ ] `BLOCKER` 平台缺料/替代清单已经逐项人工批准。
- [ ] 不允许关键 IC、连接器和 FPC 座静默替代。
- [ ] 已确认哪些连接器 SMT，哪些到板后手焊。
- [ ] 首批标准主板数量：`____`（建议 2～5）。
- [ ] 首批扩展板数量：`____`（建议 1～2）。
- [ ] 云台板本批：否。

## 板外器件

- [ ] `BLOCKER` 屏幕控制器、触控控制器、18P FPC 引脚和触点方向确认。
- [ ] `BLOCKER` 摄像头为 BF3901 SPI，24P FPC 定义与固件/主板一致。
- [ ] 麦克风极性和机械尺寸确认。
- [ ] 喇叭阻抗、功率、尺寸和引线方式确认。
- [ ] 标准外壳、金属轴、扭簧和 M1.7 × 5 螺丝已列入采购清单。
- [ ] 锂电池未纳入首次上电测试。

## 到板准备

- [ ] 有万用表、限流 USB 电源或实验电源。
- [ ] 有放大检查条件和基础返修工具。
- [ ] 已准备最小测试固件和完整 ESP-CLAW 固件。
- [ ] 已建立实验日志并准备记录电源轨、电流、温升和 USB 日志。

## 批准

```text
工程 revision：
审核日期：
审核人：
未关闭风险：
决定：下单 / 暂缓
```
